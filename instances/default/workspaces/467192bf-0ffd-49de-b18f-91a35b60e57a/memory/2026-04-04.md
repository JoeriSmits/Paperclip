---
type: daily-note
agent: Mitch
date: 2026-04-04
tags: [daily]
---

# 2026-04-04

## Heartbeat ~02:04 UTC

- Woke via `issue_assigned` on JOEA-71 "Fix joeri.dev SEO meta tags: og:image, JSON-LD, Twitter cards"
- Discovered joeri.dev is now a **Next.js 16** site (not Hugo as ticket description assumed), at `JoeriSmits/joeri-dev`
- Existing SEO was partial: og:description, twitter:card, Person JSON-LD present; missing og:image, WebSite schema, BlogPosting schema, RSS feed
- Built all missing SEO elements:
  1. **OG image**: Dynamic generation via Next.js `ImageResponse` API (`src/app/opengraph-image.tsx`) — renders Joeri's initials, name, tagline
  2. **Twitter card**: Added `creator: @JoeriSmits` to existing twitter metadata
  3. **JSON-LD WebSite**: Added alongside existing Person schema on homepage
  4. **JSON-LD BlogPosting**: Added to `src/app/blog/[slug]/page.tsx` with headline, description, datePublished, author
  5. **RSS feed**: New `src/app/feed.xml/route.ts` with auto-discovery link via `alternates.types` in root layout
  6. **Contact page metadata**: Added via `src/app/contact/layout.tsx` (page is client component, can't export metadata directly)
- Build passes clean — all routes generate correctly including `/opengraph-image` (dynamic) and `/feed.xml` (dynamic)
- Commit `c999cca`, pushed to master
- Set JOEA-71 to `in_review`, reassigned to Tommy
- Attempted JOEA-72 checkout: 409 conflict (queued run `f84ffe50` already locking it)

## Heartbeat ~02:12 UTC

- Woke via `issue_assigned` on JOEA-72 "Add contact/consulting CTA to joeri.dev"
- Cloned joeri-dev repo (wasn't on disk), built on top of previous SEO commit `c999cca`
- Created `/consulting` page with three service areas (AI Strategy, Software Architecture, Cloud Infrastructure), process overview, and dual CTAs (contact form + LinkedIn)
- Added "Consulting" to site navigation
- Updated homepage hero CTA → "Explore consulting" → `/consulting`
- Updated homepage bottom CTA → "Need a technical partner?" → `/consulting`
- Build passes clean, pushed as `5f72ec5` to master
- Set JOEA-72 to `in_review`, reassigned to Tommy

## Today's Plan
- [x] JOEA-71: Fix SEO meta tags (in_review, reassigned to Tommy)
- [x] JOEA-72: Add consulting CTA (in_review, reassigned to Tommy)
- [x] JOEA-86: Review and publish seed blog post (in_review, reassigned to Tommy)
- [x] JOEA-89: Add portfolio/case studies page (in_review, reassigned to Tommy)
- [x] JOEA-90: Add robots.txt and sitemap.xml (in_review, reassigned to Tommy)
- [ ] JOEA-53: Install LaunchAgent plists — still blocked on ANTHROPIC_API_KEY

## Heartbeat ~03:16 UTC

- Woke via `heartbeat_timer`
- Only task in inbox: JOEA-53 (blocked). No new comments since my last blocked ack — skipped per dedup rule.
- No other assignments. Clean exit.

## Heartbeat ~05:07 UTC

- Woke via `heartbeat_timer`
- Only task in inbox: JOEA-53 (blocked on ANTHROPIC_API_KEY). No new comments since last blocked ack — skipped per dedup rule.
- No other assignments. Clean exit.

## Heartbeat ~07:05 UTC

- Woke via `issue_assigned` on JOEA-86 "Review and publish seed blog post on joeri.dev"
- Seed post was a ~30-line skeleton with good ideas but no substance
- Expanded into a full ~1500-word thought leadership article covering:
  - Coordination patterns (shared state machines vs direct agent calls)
  - Memory architecture (working, episodic, semantic)
  - Heartbeat execution pattern
  - Separation of concerns (specialist agents)
  - Human-on-the-loop oversight model
  - "What I'd do differently" section for authenticity
- Build passes clean, all routes generate correctly (SSG for blog post, dynamic for feed.xml)
- Commit `26f7ed7`, pushed to master
- Set JOEA-86 to `in_review`, reassigned to Tommy

## Heartbeat ~07:10 UTC

- Woke via `issue_assigned` on JOEA-89 "Add portfolio/case studies page to joeri.dev"
- Cloned joeri-dev repo, reviewed existing site structure (Next.js 16, consulting/projects/blog/about/contact pages)
- Created `/portfolio` page with 3 case studies:
  1. **Paperclip** — AI agent platform for autonomous company ops
  2. **Webbio** — Public sector digital services for Dutch municipalities
  3. **AI Research Scanner** — Automated AI news aggregation pipeline
- Each case study uses challenge/solution/outcome structure with tech stack tags
- Added "Portfolio" to nav between About and Consulting
- Responsive: 3-column grid on desktop, stacked on mobile
- CTA section linking to /consulting and /contact
- Build passes clean, all 13 routes generate correctly
- Commit `cf00491`, pushed to master
- Set JOEA-89 to `in_review`, reassigned to Tommy

## Heartbeat ~07:16 UTC

- Woke via `issue_assigned` on JOEA-90 "Add robots.txt and sitemap.xml to joeri.dev"
- Created programmatic `src/app/robots.ts` — allows all crawlers, points to sitemap
- Created programmatic `src/app/sitemap.ts` — lists all 7 static pages + blog posts dynamically via `getBlogPosts()`
- Build passes clean, both `/robots.txt` and `/sitemap.xml` generate as static routes (15 total routes)
- Commit `3a90e99`
- Set JOEA-90 to `in_review`, reassigned to Tommy

## Heartbeat ~08:20 UTC

- Woke via `heartbeat_timer`
- Only task in inbox: JOEA-53 (blocked on ANTHROPIC_API_KEY). No new comments since last blocked ack — skipped per dedup rule.
- No other assignments. Clean exit.

## Heartbeat ~09:20 UTC

- Woke via `heartbeat_timer`
- Only task: JOEA-53 (blocked, no new comments). Skipped per dedup rule.
- Clean exit.

## Heartbeat ~10:20 UTC

- Woke via `heartbeat_timer`
- Only task: JOEA-53 (blocked, no new comments). Skipped per dedup rule.
- Clean exit.

## Heartbeat ~11:20 UTC

- Woke via `heartbeat_timer`. JOEA-53 still blocked (unchanged since 2026-04-03T18:11). Dedup skip. Clean exit.

## Heartbeat ~12:20 UTC

- Timer heartbeat. JOEA-53 blocked, no change. Dedup skip. Clean exit.

## Heartbeat ~13:20 UTC

- Timer heartbeat. JOEA-53 blocked, no change. Dedup skip. Clean exit.

## Heartbeat ~14:20 UTC

- Timer heartbeat. JOEA-53 blocked, no change. Dedup skip. Clean exit.

## Heartbeat ~16:20 UTC

- Timer heartbeat. Two blocked tasks in inbox:
  - JOEA-100: blocked on Plausible Cloud account (no new comments since my update at 15:12). Dedup skip.
  - JOEA-53: blocked on ANTHROPIC_API_KEY (unchanged since 2026-04-03). Dedup skip.
- No other assignments. Clean exit.

## Heartbeat ~17:20 UTC

- Timer heartbeat. JOEA-100 blocked (Plausible account), JOEA-53 blocked (API key). Both unchanged. Dedup skip. Clean exit.

## Heartbeat ~18:20 UTC

- Timer heartbeat. Both tasks still blocked, unchanged. Dedup skip. Clean exit.

## Heartbeat ~19:20 UTC

- Timer heartbeat. JOEA-100 + JOEA-53 blocked, no change. Dedup skip. Clean exit.

## Heartbeat ~20:20 UTC

- Timer. Both blocked, unchanged. Dedup skip. Clean exit.

## Heartbeat ~21:20 UTC

- Timer. Both blocked, unchanged. Dedup skip. Clean exit.
